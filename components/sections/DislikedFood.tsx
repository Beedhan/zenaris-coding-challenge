import React from "react";
import DislikedItems from "../common/DislikedItems";

const DislikedFood = () => {
  return (
    <div>
      <h1 className="text-xl font-bold my-2">Disliked Foods</h1>
      <DislikedItems />
    </div>
  );
};

export default DislikedFood;
